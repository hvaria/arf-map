/**
 * Legacy import shim. NotesContent now lives in
 * @/components/operations/NotesContent. This file re-exports it so existing
 * import paths keep working until Phase D removes the wrappers.
 */
export { NotesContent } from "@/components/operations/NotesContent";
